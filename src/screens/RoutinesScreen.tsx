import React from 'react';import {Pressable,ScrollView,StyleSheet,Text,View} from 'react-native';import {NativeStackNavigationProp} from '@react-navigation/native-stack';import {useNavigation} from '@react-navigation/native';import {SafeAreaView} from 'react-native-safe-area-context';import {useAppDispatch,useAppSelector} from '../app/hooks';import {startRoutine} from '../features/progress/progressSlice';import {colors} from '../theme/colors';import {spacing} from '../theme/spacing';import {RootStackParamList} from '../types/navigation';
export function RoutinesScreen(){const nav=useNavigation<NativeStackNavigationProp<RootStackParamList>>();const d=useAppDispatch();const routines=useAppSelector(s=>s.progress.routines);const steps=useAppSelector(s=>s.progress.routineSteps);const runs=useAppSelector(s=>s.progress.routineRuns);const start=async(id:string)=>{await d(startRoutine(id)).unwrap();nav.navigate('RoutineRun',{routineId:id});};return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.content}><Pressable onPress={()=>nav.goBack()}><Text style={s.back}>‹ Habits</Text></Pressable><Text style={s.title}>Smart Routines</Text><Text style={s.subtitle}>Sequences that reduce daily decision fatigue.</Text>{routines.map(r=>{const count=steps.filter(x=>x.routineId===r.id).length;const active=runs.find(x=>x.routineId===r.id&&x.status==='active');return <View key={r.id} style={s.card}><Text style={s.icon}>{r.icon}</Text><View style={{flex:1}}><Text style={s.name}>{r.name}</Text><Text style={s.meta}>{count} steps · {r.context}</Text></View><Pressable onPress={()=>active?nav.navigate('RoutineRun',{routineId:r.id}):void start(r.id)} style={s.button}><Text style={s.buttonText}>{active?'Resume':'Start'}</Text></Pressable></View>})}</ScrollView></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},content:{padding:spacing.md,paddingBottom:80,gap:spacing.md},back:{color:colors.accent,fontWeight:'800'},title:{color:colors.text,fontSize:30,fontWeight:'900'},subtitle:{color:colors.textMuted,marginTop:-10},card:{flexDirection:'row',alignItems:'center',gap:12,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,borderRadius:20,padding:spacing.md},icon:{fontSize:28},name:{color:colors.text,fontWeight:'900',fontSize:17},meta:{color:colors.textMuted,fontSize:12,marginTop:5},button:{backgroundColor:colors.accentSoft,borderRadius:12,paddingHorizontal:14,paddingVertical:10},buttonText:{color:colors.accent,fontWeight:'900'}});
